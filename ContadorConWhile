#include <iostream>
using namespace std;
int main()
{
    int cont, a; // CONT=0
    cout<<"Ingresa el numero de repeticiones"<<endl;
    cin>>a;
    cont=0;
    while (cont<a)
    {
        cont=cont+1;
    }
    
    cout<<"El valor del contador es: "<<cont;

    return 0;
}
